#' @title  Known compound entries
#' @description  Compunds that should be shown when database is selected as a target signal specification method. Compounds are stored in a csv file, which is read-in when the app is started. Use the provided example file (path ***system.file("extdata/", package = "MetaboMate")***) as a template.

